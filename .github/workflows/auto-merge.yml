name: Auto-merge to main

on:
  push:
    branches:
      - 'feature/*'
      - 'improve/*'

jobs:
  test-and-merge:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Frontend tests
        run: |
          npm ci
          npm test

      - name: Backend tests
        run: |
          cd server
          npm install
          npm test

      - name: Merge to main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -f main
          git merge origin/${{ github.ref_name }} --no-ff -m "Auto-merge: ${{ github.ref_name }} → main"
          git push origin main
